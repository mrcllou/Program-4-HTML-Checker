Program #4: HTML Checker

Assignment: We will be creating an HTML Checker program that will check the syntax of HTML tags. 
Write a class that stores the contents of an HTML page as a queue of HTML tags. Class able to fix invalid HTML tags. 
HTMLManager uses stacks and queues to figure out whether the tags match and fix the mistakes it finds. 


